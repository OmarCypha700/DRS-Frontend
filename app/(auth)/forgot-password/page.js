import { ForgotPasswordForm } from "@/features/auth/forgot-password-form";

export const metadata = { title: "Forgot password — Document Request System" };

export default function ForgotPasswordPage() {
  return <ForgotPasswordForm />;
}
